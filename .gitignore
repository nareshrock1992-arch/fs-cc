# Dependencies (never track — restore via `npm install` from the lockfile)
node_modules/

# Build output (regenerated by `npm run build`)
dist/
build/

# Environment / secrets
.env
.env.*
!.env.example

# Logs & runtime
*.log
npm-debug.log*
pids/
*.pid

# OS / editor
.DS_Store
Thumbs.db
*.swp
